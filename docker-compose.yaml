services:
  claude-dev:
    image: ghcr.io/lostmaniac/claude-dev:latest
    container_name: claude-dev
    ports:
      - "2222:22"
    volumes:
      - ./root:/root
      - ./data:/data
    restart: unless-stopped
